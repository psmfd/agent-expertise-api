## .NET
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs

## NuGet
**/[Pp]ackages/*
!**/[Pp]ackages/build/
*.nupkg
**/[Pp]ackages/*.config

## Build results
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

## Visual Studio
.vs/
*.csproj.user
*.dbmdl
*.jfm

## Rider
.idea/
*.sln.iml

## VS Code
.vscode/

## OS
.DS_Store
Thumbs.db

## Environment
.env
*.local.json
!appsettings.*.json

## ONNX models (large binaries — downloaded at build time). Pattern intentionally
## targets the artifact filenames rather than the directory, because on case-insensitive
## filesystems (macOS HFS+/APFS with core.ignorecase=true) a `src/ExpertiseApi/models/`
## directory rule would also match the C# `src/ExpertiseApi/Models/` namespace directory
## and silently ignore new `.cs` files added under it.
src/ExpertiseApi/Models/model.onnx
src/ExpertiseApi/Models/vocab.txt
src/ExpertiseApi/Models/*.bin
src/ExpertiseApi/Models/tokenizer*.json

## Claude Code worktrees
.claude/worktrees/

## Claude Code per-agent runtime memory (subagent scratch; not part of the repo)
.claude/agent-memory/

## Part D C8: build-time OpenAPI document emission (Microsoft.Extensions.ApiDescription.Server)
## Output of _GenerateOpenApiDocuments MSBuild target; published as a GitHub Release asset via release.yml.
src/ExpertiseApi/artifacts/

## pi extension node_modules
.pi/extensions/*/node_modules/

## Python bytecode cache (scripts/mint_token.py and any future helper scripts)
__pycache__/
*.pyc

## mint_token.py (ADR-015) private key material — NEVER commit. The default KEY_DIR is
## ./keys and per-client private keys are *.priv.json ("d" JWK material the PEM-oriented
## secrets-guard does not detect). The API only ever loads the PUBLIC build-jwks output.
keys/
*.priv.json
